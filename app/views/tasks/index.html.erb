<div class="container">
  <h1>My tasks</h1>
  <% @tasks.each do |task|%>
    <h3> <%= link_to task.title, task_path(task) %> <%= link_to "(edit)", edit_task_path(task) %> </h3>
    <p> <%= task.completed %> </p>
    <% end %>

  <%= link_to "New task", new_task_path %>

</div>
